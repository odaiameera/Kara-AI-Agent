[project]
name = "personal-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "openpyxl>=3.1.5",
    "python-docx>=1.2.0",
    "python-dotenv>=1.2.2",
    "python-pptx>=1.0.2",
    "python-telegram-bot>=21.0",
]

[project.scripts]
kara = "agent:main"
kara-gateway = "gateway:main"
kara-update = "update:main"
kara-install = "install_gateway:main"
kara-codex-auth = "codex_auth:main"
