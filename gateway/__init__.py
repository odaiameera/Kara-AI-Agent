"""Kara gateway package — long-running daemon and platform adapters.

STUDY GUIDE
-----------
* Marks ``gateway/`` as a Python package so you can ``import gateway.run``, etc.
* Empty ``__init__.py`` files are valid: the folder name becomes an importable namespace.
* Key concepts: Python packages vs modules, package layout, implicit namespace imports.
"""
